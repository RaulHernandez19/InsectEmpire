export class insectInfo {
  name!: string;
  taxonKey!: number;
  img!: string;
  country!: string;
  size?: string; // Nuevo campo para las medidas
  description?: string; // Nuevo campo para la descripción
}
